<%= render 'masthead' %>

<%
  require 'lexicon'
  lexicon = Lexicon.new('app/views/welcome/demo.json')
%>
<%= stylesheet_include_tag('demo') %>
<%= javascript_include_tag('demo') %>

<h1> <%= lexicon.get('title') %> </h1>

<ul>
  <li>Lexicon-Ruby version: <%= Lexicon::VERSION %></li>
  <li>Lexicon-JS version: <span class="LexiconJsVersion"/></li>
  <li>How is Lexicon linked? <pre><%= @how_is_lexicon_module_linked %></pre></li>
</ul>

<h3>Using Lexicon from Ruby:</h3>
<%= lexicon.get('introduction') %>

<h3>React component:</h3>
<div class="CookieAuthenticatedDemo"> (This should be replaced by <Demo> component) </div>

<h3>Same React component but using different Lexicon:</h3>
<div class="JwtAuthenticatedDemo"> (This should be replaced by <Demo> component) </div>

<h3>Same React component but using different Lexicon:</h3>
<div class="PigLatinDemo"> (This should be replaced by <Demo> component) </div>
